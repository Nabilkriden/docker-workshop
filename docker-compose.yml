version: "3.8"
services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./:/usr/src/app   # bind mount source for live edits
      - /usr/src/app/node_modules # avoid overwriting installed modules
    environment:
      - NODE_ENV=development
